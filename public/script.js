// Initialize Fomantic UI Components when the page loads
$(document).ready(function () {
  
  //  Initialize Checkbox Components
  // Enables custom checkboxes with `.ui.checkbox` (e.g., toggle switches, checkboxes)
  $('.ui.checkbox').checkbox();
  
  //  Initialize Dropdown Menus
  // Enables dropdown functionality for elements with `.selection.dropdown` class
  $('.selection.dropdown').dropdown();

});
